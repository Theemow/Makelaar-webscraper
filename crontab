# Set environment variables
PATH=/usr/local/bin:/usr/bin:/bin
DOCKER_ENVIRONMENT=true

# Run the webscraper at 12:00 and 18:00 every day - logs go to stdout/stderr
# This allows Docker's logging drivers to capture the output
0 12,18 * * * cd /app && python /app/huurhuis_webscraper.py 2>&1
# New line character required!